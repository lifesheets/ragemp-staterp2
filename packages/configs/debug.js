"use strict"; // Debug Mode (Created by ua.lifesheets on 02.02.2023).

let config = {
    'Client': {
        'Console': false,
        'SaveFile': false
    },
    'Server': {
        'Console': false,
        'SaveFile': false
    }
}

module.exports = config;